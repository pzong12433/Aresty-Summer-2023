To open DLC:

1. Search for and open Anaconda Prompt in Windows search bar. (You should see '(base) C:\Users\Mark>')

2. Type "activate deeplabcut" ('(base)' should become '(deeplabcut)')

3. Type "ipython" ('(deeplabcut)' should become 'In [1]:' in green)

4. Type "import deeplabcut" ('Loading DLC...' should appear followed by 'In [2]:' in green)

To start a new project:

1. 
